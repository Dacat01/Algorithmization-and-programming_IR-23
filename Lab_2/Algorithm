АЛГОРИТМ РОБОТИ:

1. Перевіряєм чи можуть всі хом'яки жити разом.
   Якщо так, то функція повертає значення N.

2. Чи можуть жити разом хом'яки кількістю  (>1 && <N):

- На кожному кроці циклу заносимо  кількість їжі, яку споживає
кожен  з хом'яків, якщо будуть жити разом (N-I) хом'яків
в масив розміром (N-I). Залишається I хом'яків,
які не влазить в масив, того здійснюється почергова перевірка:
чи кількість споживаної їжі кожного з них є меншою за елемент з
найбільшою її кількістю в масиві. Якщо так, то 
він заноситься в масив на  його місце.

- Опісля обраховується сума споживаної їжі  хом'яків у масиві. 
Якщо запасів їжі(S) більше, або ж достатньо щоб їх прогодувати,
то функція повертає значення (N-I)

3. Якщо ж (>1 && <N) не можуть жити разом, то здійснюється
перевірка чи  хоч 1 хом'як може прожити на  запас їжі (S).
Якщо так, то функція повертає значення 1.

4. Якщо жоден з хом'яків не  може прожити на 
запас їжі(S) - повертається значення 0.


